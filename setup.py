from setuptools import find_packages, setup

setup(name='datasetmaker',
      version='0.1.0',
      description='Abstract base class for data pipelines',
      url='#',
      author='Elliot Hill',
      author_email='',
      packages=find_packages(include=['datasetmaker']),
      zip_safe=False)
